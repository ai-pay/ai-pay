For more information on the OpenAI Embeddings API, please visit the [official OpenAI documentation](https://platform.openai.com/docs/guides/embeddings)

## Supported Models
- `text-embedding-3-small`
- `text-embedding-3-large`

Note: If you would like to see other models supported by AI Pay, please submit feedback or reach out the the team at marcus@joinaipay.com.


import { EmbeddingsExample } from "../../examples/embeddings";

## Example
<EmbeddingsExample>
```typescript
import { embeddings } from "ai-pay/apis"
import { EmbeddingsRequest } from "ai-pay/models";

async function callChatCompletion(contextPrompts: string[]) {  
  const request: EmbeddingsRequest = INPUT;

  const {
    error,
    debugError,
    data,
  } = await embeddings(request);

  if (error) {
    updateUiWithError(error);
    console.log("AI Pay embeddings debugError", debugError)
  } else {
    const consineSimilarity = calculateSimilarity(data[0].embedding, data[1].embedding)
    updateUiWithResponse(response, consineSimilarity)
  }
}
```
</EmbeddingsExample>
